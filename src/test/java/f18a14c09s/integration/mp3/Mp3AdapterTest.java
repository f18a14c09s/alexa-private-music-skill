package f18a14c09s.integration.mp3;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class Mp3AdapterTest {
    @Test
    void testParseMetadata() {
//        Mp3Adapter subject = new Mp3Adapter();
//        TrackMetadata trackMetadata = assertDoesNotThrow(() -> subject.parseMetadata(getClass().getResourceAsStream(
//                "/example-data/smooth-operator_sade_diamond-life.mp3")));
//        assertNull(trackMetadata.getComment());
//        assertNull(trackMetadata.getCopyright());
//        assertEquals((Long) 296L, trackMetadata.getDurationSeconds());
//        assertEquals("Diamond Life", trackMetadata.getAlbum());
//        assertEquals("Sade", trackMetadata.getAuthor());
//        assertEquals("1984", trackMetadata.getYear());
//        assertEquals("Smooth Operator", trackMetadata.getTitle());
    }
}
